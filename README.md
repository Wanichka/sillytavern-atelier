# Wani Atelier — 0.2.0

Редактор оформления SillyTavern: тема оформления назначается персонажу и
переключается вместе с ним. Работает без Moonlit Echoes.

## Как устроено

**Тема** — именованный набор: цвета, фон, шрифт, макет сообщений, портреты.
Название любое: «Морская», «Ло · универ», что угодно.

Тему можно назначить персонажу. Одна тема может стоять у нескольких карточек —
правишь один раз, меняется у всех. Нужна отдельная — нажми «Дублировать».

Персонажу без назначенной темы применяется **тема по умолчанию**. Если и её нет,
Таверна остаётся в своём обычном виде.

Персонаж определяется файлом аватарки, поэтому двадцать карточек с именем «Ло»
не путаются между собой.

## Установка

Распакуй папку `sillytavern-atelier` в
`SillyTavern/public/scripts/extensions/third-party/` и обнови страницу.
Это обычное клиентское расширение, серверные плагины не нужны.

Перед проверкой отключи Moonlit и перезагрузи страницу: две полные темы
одновременно не работают. Оформление не применяется, пока не включён флажок
«Применять оформление Atelier» — он же снимает всё обратно.

Если стоит Roleplay Tools, редактор появится на странице «Оформление» одной
колонкой. Для двух колонок с предпросмотром справа отключи сборку расширений
в общую панель.

## Настройки из старой версии

Формат версии 1 переносится сам: общая тема становится «Общей» и темой по
умолчанию, персональные — «Перенесённая тема 1…N» с сохранёнными привязками,
пресеты — обычными темами под своими именами. Названия макетов `ripple` и
`opposite` переезжают на новые.

## Шрифты

В списке одиннадцать заготовок. Кнопка «Взять шрифты с компьютера» подтягивает
все установленные в системе — работает в Chrome и Edge от 103-й версии, спросит
разрешение. В Firefox, на Android и при заходе по локальному IP вместо
localhost список недоступен: там название пишется вручную, поле рядом.

Шрифт должен стоять на той машине, где ты смотришь чат. На планшете тема с
локальным шрифтом откатится на запасной.

## Что ещё не сделано

Каталог персонажей; отдельные настройки портретов для пользователя и персонажа;
загрузка файлов шрифтов; импорт тем Moonlit и штатных тем UI; тени и размытие
панелей; расширенный пользовательский CSS; отдельные темы для чатов и групп —
у группового чата применяется тема по умолчанию.

## Проверки

`npm test` — модель данных: общая тема на нескольких персонажах, откат к теме по
умолчанию, битые ссылки, перенос со старого формата, отбрасывание опасных
значений при импорте, чистка названий, уникальность идентификаторов,
непрозрачность и разбор ответа альбома фонов.

Браузерный сценарий прогоняется в jsdom: создание темы, правка цвета через поле
hex, назначение персонажу, переключение между двумя карточками с одинаковым
именем, сохранение, кадрирование портрета, зависимые поля макета, свой шрифт.
