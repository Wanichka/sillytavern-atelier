# Wani Atelier — 0.1.0 alpha

Первая техническая версия самостоятельного редактора оформления SillyTavern. Это начало реализации ТЗ, не законченная замена Moonlit.

## Реализовано

- Редактор со вкладками слева, круглой перемещаемой кнопкой и локальным предпросмотром.
- Сохранение в extensionSettings текущего пользователя Таверны, общая тема и отдельные темы карточек по имени файла аватарки. Обрабатывается событие переименования карточки.
- Независимые черновики, сохранение и отмена. При переходе к другой карточке применяется её сохранённая тема, черновик предыдущей остаётся до перезагрузки.
- Цвета, шрифты, геометрия сообщений, четыре начальных макета: боковой, двусторонний, компактный, портрет над текстом.
- Просмотр и загрузка в существующий альбом фонов через API Таверны. Поддерживаются ответы all в виде массива строк и объекта images с filename.
- Пресеты и импорт/экспорт JSON настроек; изображения в файл не включаются.
- Адаптер Roleplay Tools API v1, вкладка «Оформление». Подключается при любом порядке загрузки, перемещает существующий редактор без копий и сохраняет самостоятельный режим.

## Установка для теста

Пока репозиторий не создан, распакуйте папку `sillytavern-atelier` в `SillyTavern/public/scripts/extensions/third-party/`, перезагрузите страницу. Это обычное клиентское расширение; серверные плагины не нужны.

Отключите Moonlit в настройках расширений и перезагрузите страницу перед проверкой. Atelier по умолчанию не применяет оформление: откройте его и включите «Применять оформление Atelier». Собственное включение снимается этим же флажком. Настройки остаются сохранены.

Если установлен Roleplay Tools API v1, редактор появится на странице «Оформление». Положение общей панели выбирается в Roleplay Tools; для независимого левого редактора отключите сборку расширений в его общую панель. Исходники Roleplay Tools не требуют изменений.

## Ещё не реализовано

Каталог персонажей; полное объединение всех стандартных настроек UI; полная синхронизация с изменениями в штатном меню; шрифты из файлов; импорт Moonlit; декоративное обтекание и затухание портретов; отдельные размеры пользователя и персонажа; предпросмотр отдельной правой колонкой (в alpha он ниже настроек, чтобы помещаться в Roleplay Tools); законченный дизайн всех макетов. Размытие фона пока применяется к настоящему чату, не к картинке предпросмотра. Загруженный фон сразу доступен нашему альбому; штатный альбом при необходимости обновляется перезагрузкой страницы.

Изменения снимутся при отключении расширения и перезагрузке. Не изменяются файлы карточек, история, промты или базовая тема; применяется слой CSS. Сохранность несохранённых черновиков между перезагрузками не обещается.

Не проверено в подключённой пользовательской Таверне. Перед основным использованием проверить реальную сборку, регекс-блоки, свайпы и потоковый ответ. Макеты пока прототипные.

## Проверки

`npm test` — изоляция одинаковых имён карточек, наследование общей темы, независимость копий, валидация импортируемых значений, форматы альбома.

Браузерный стенд проверяет код расширения с имитацией контекста/API Таверны и настоящим скриптом Roleplay Tools (fixture из Wanichka/sillytavern-roleplay-tools, commit blob f2517bad55f15cba44ddeb8a48dee0a3dc2bc5aa). Проверены предпросмотр до сохранения, сохранение и перезагрузка, переключение тем, альбом, dock/release, отключение стилей, кнопка и ширина мобильного окна.

Для воспроизведения browser.mjs нужны Playwright, Chromium и путь к его исполняемому файлу в CHROMIUM_PATH; зависимости в рабочей среде Codex задаются CODEX_PRIMARY_RUNTIME_NODE_MODULES. Этот стенд пока подготовлен для среды разработки, не является установщиком расширения.
